name: deploy-web

on:
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'tools/**'
      - '.github/workflows/deploy-web.yml'
      - 'package.json'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci --no-audit --no-fund
      # The contrast suite gates the deploy. An unreadable palette must never
      # reach production just because it compiled.
      - run: node tools/check-contrast.mjs
      - run: npm run test --workspaces --if-present
      # Set VITE_GOATCOUNTER as a repository variable (Settings > Secrets and
      # variables > Actions > Variables) to switch analytics and the visitor
      # counter on. Unset, the site builds with neither and nothing breaks.
      - run: npm run build --workspace apps/web
        env:
          VITE_GOATCOUNTER: ${{ vars.VITE_GOATCOUNTER }}
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
