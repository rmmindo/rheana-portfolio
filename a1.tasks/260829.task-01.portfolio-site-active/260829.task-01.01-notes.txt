Task ID: 260829.task-01
File:    working notes and decisions

================================================================================
DECISIONS AND WHY
================================================================================

2026-08-29  Domain moved rather than re-registered.
  rheanamindo.me already had correct DNS on all four GitHub Pages apex IPs.
  Claiming a new .tech domain would have restarted certificate provisioning on
  the critical path for no gain. The certificate issued in under two minutes.

2026-08-29  Site content generated from raysume, not retyped.
  The April PDFs in Downloads and the August raysume YAML disagreed: 759 PRs
  versus 2,320, and 116 Claude Code skills versus 103. Generating the site from
  the same source as the PDFs makes that class of drift impossible.

2026-08-29  Pastels are surfaces, never text.
  Measured: every one of the six fails WCAG as text on white, between 1.21 and
  3.35 against a 4.5 requirement. All six pass as surfaces with dark ink. Hence
  the two-tier token system and the contrast suite that gates the deploy.

2026-08-30  Fly.io abandoned for the visitor counter.
  Fly now requires a card on file even inside the free allowance. GoatCounter is
  free without one, sets no cookies, and stores no personal data, so the privacy
  property that justified building the Express service survives the move.
  apps/api stays in the repo as the tested reference implementation.

2026-08-30  Template matching, not a model, for Baybayin recognition.
  A trained model would handle messy input better and would mean shipping
  weights and a runtime to every visitor for a toy. Templates are rasterised
  from the font the page already loads: nothing to download, deterministic, and
  wrong in ways you can explain by looking at the shapes.

2026-08-30  The reference site was studied properly, with Playwright.
  Earlier rounds worked from a text summary of justus-john.com, which is why
  repeated tweaking never converged. Measured: Instrument Serif at 90 to 102px
  with line-height 1.0, body at 22 to 27px, one accent used 91 times against
  1069 uses of near-black, and sections 7 to 11 screens tall. It is a sequence
  of scroll-driven scenes, not a stack of sections.

================================================================================
BUGS FOUND AND FIXED
================================================================================

B-01  Reveal threshold hid every tall section on mobile.
      threshold 0.15 is a fraction of the OBSERVED ELEMENT. The Experience
      section is several screens tall on a phone, so 15 percent of it could
      never be visible and the observer never fired. Everything stayed at
      opacity 0. Fixed with threshold 0, plus a js-reveal class so the hidden
      state only exists while JavaScript is running, plus a 3 second failsafe.

B-02  Reveal specificity hid the trust bar entirely.
      .js-reveal .trust is 0-2-0; .is-revealed is 0-1-0. The hidden rule won and
      the section sat at opacity 0 as 380px of blank page. Cards escaped only
      because .is-revealed .entry is also 0-2-0.

B-03  Sass colour-keyword map keys.
      purple, pink and red are CSS colour keywords. Unquoted as map keys they
      interpolated as colour VALUES, producing malformed custom property names.

B-04  A stray line under every card.
      drop-shadow with a 2px offset painted a solid copy of the card two pixels
      down. Because the card is masked into a scallop, the copy only showed
      along the bottom and read as a line sitting under the card.

B-05  The vine flattened into a straight stroke.
      A 1000-unit viewBox with preserveAspectRatio none stretched down a
      15,000px page turns four S-curves into a vertical line. Later removed.

B-06  The vision gate could flash at a reduced-motion user.
      useReducedMotion resolves in an effect and reports false on the first
      pass. The gate now reads the media query directly at the moment it decides
      whether to open.

B-07  Odometer sat on zero when IntersectionObserver never fired.
      Failsafe added. A correct number late beats a wrong number indefinitely.

B-08  Entity escapes rendered as text.
      raysume HTML-escapes every value, so ampersands arrived escaped. Fields
      that bypassed the RichText decoder printed the escape sequence on screen.

B-09  Hero name ran together as one word.
      The separating space sat INSIDE an inline-block with overflow hidden, so
      it collapsed.

B-10  The at-work photograph showed the top of her head.
      A 2:3 original cropped to 2:1 with position top. Replaced with a named
      extract window after rendering three candidates and looking at them.

================================================================================
MISTAKES MADE
================================================================================

M-01  Edited the recommendations. Corrected grammar, fixed a pronoun, and split
      each into a pull-quote plus a remainder. They are other people's words and
      were not mine to tidy. Restored verbatim and guarded with tests. See R-01.

M-02  Designed for several rounds without ever looking at the site Rheana kept
      pointing at, working from a text summary instead. Playwright was available
      the whole time.

M-03  Took the site down for about four minutes by toggling the custom domain
      off and on to re-trigger a www certificate, which dropped the live
      deployment. Do not touch Pages domain settings on a live site.

M-04  Built toward the Pinterest moodboard while Rheana kept citing a monochrome
      reference, and did not name the contradiction for several rounds.

B-11  The change widget printed its unit twice.
      "2% to 5%" rendered as "2 -> 5%  %". The unit was built by deleting every
      number from the string and tidying what was left, which leaves both
      percent signs behind. The unit now comes from the position of the second
      number only.

B-12  The timer wrote state during render.
      DashboardDemo called setMs inside useMemo. Besides being a render-phase
      update, the number it showed described the previous keystroke. The memo
      now returns the widgets and the elapsed time together.

B-13  A negative value erased its bar.
      Bar height was value divided by the largest value, so a negative number
      produced a negative CSS height and the bar disappeared instead of showing
      a dip. barHeights() now measures from a baseline that always includes
      zero, and reduces rather than spreading, because Math.max(...series) on a
      long pasted series overflows the call stack.

B-14  The tested layout was not the rendered layout.
      layout() returned a row for every widget and the component never used it.
      The rows the tests assert are now written to grid-row.

B-15  The widget count announced itself on every keystroke.
      aria-live on the timer meant a screen reader read "6 widgets, <1 ms" over
      the user's own typing. Removed. The count is visible; it does not need to
      interrupt.

B-16  The unit could be read off the wrong number.
      indexOf("3") inside "3 out of 30" finds the digit inside 30 and returned
      "0" as the unit. Parsing now uses matchAll and keeps each match position.

B-17  The dashboard rendered two-thirds empty.
      First fit left the third column stranded on every row after the first,
      because a two-column chart cannot follow two one-column stats. The last
      widget on each row now absorbs the leftover columns. Widening rather than
      reordering: CSS dense packing would pull a later widget forward, and the
      order a screen reader follows would stop matching the order on screen.

M-05  Wrote the note under the dashboard as three long sentences, which is the
      word vomit R-02 exists to prevent. Cut to two.

B-18  The carousel moved out from under the pointer.
      The dwell timer was rendered only while the carousel was running, so
      hovering a card unmounted it, removed its 22px of height, and shifted the
      whole track upwards. The mousedown and the mouseup then landed on
      different elements and the click never fired: the read-more button simply
      did nothing to a real mouse, while a scripted click worked fine. The
      element is now always rendered and only faded, so the space it occupies
      never changes.

B-19  The tokeniser kept the sentence's punctuation.
      "merge." did not match a search for "merge", because the pattern allowed
      a trailing dot so that "node.js" and "ci/cd" survive. Trailing punctuation
      is now stripped and internal punctuation kept. Caught by a test, not by
      looking.

B-20  One card per view was not one card per view.
      The track was full-bleed with the first card aligned to the content
      column, which is what made the next card peek in. Constraining the track
      to the column and forcing the card to its full width fixed it. The card
      also needed an explicit width, not only a flex-basis, because the
      figure's own max-width was otherwise deciding how wide the card was.

M-06  Reported that the CSS clamp was broken because the box was shorter than
      its content. That IS the clamp working. The real fault was B-18, which
      stopped the button ever being pressed.

M-07  Ran a build from the wrong directory, then measured the result and
      reported no change. The build had not run.

B-21  Two calls at once corrupted the model.
      A transformers.js pipeline holds one inference session. The bias probe
      naturally wanted both halves together:

        Promise.all([unmask(he), unmask(she)])
        -> TypeError: Cannot read properties of null (reading '3')

      while the same two calls awaited one after the other are fine. Proved it
      by running both forms against the library directly, outside our code,
      after the library alone worked. The SEARCH demo had the same latent
      fault: a second keystroke fired a second embedding before the first had
      finished. Both now go through withPipeline(), which queues jobs per
      pipeline, so no caller has to remember.

M-08  Diagnosed the read-more button by theory for several rounds instead of
      measuring. The answer came in one probe once it read the actual
      geometry.

B-22  The toolshed said no to the stack the site is built with.
      "Express" answered "not on the list" while the E in MERN/PERN sat right
      there, and React and Node with it - on a React site. An acronym on a CV
      is one line meaning five tools; a reader expands it without noticing and
      a substring search cannot. searchTools now knows what the acronyms stand
      for, names the tool asked about and the line it lives on, ranks a written
      chip above an unpacked one, and never lists the same tool twice. The
      resume is still the only source: MERN means those words.

D-01  The video moved from first section to after the work.
      Rheana: "what is this random video". It was: a stranger who has not seen
      one piece of work has no reason to give a person three minutes, and
      "Meet me in 3 minutes" was about her rather than about them. It now sits
      after Experience and Beyond, before the people who vouch for her, and
      says "You would be hiring a person".

D-02  The nav indicator on the hero.
      Could not reproduce a label while the hero is on screen, at 1280 and at
      390, locally or live: it is blank at every scroll position until the
      hero has left. What Rheana saw was the video being section 01. It is now
      section 03, so nothing reads as belonging to the hero.

D-03  The trust bar was removed, not rebuilt a fourth time.
      Rheana asked whether it was needed. It was not. The page already carries
      twelve figures, each attached to a story - 4,062 compile errors, 78% of
      payments missing, 61x faster, 756% budget growth - and the bar added four
      more with none. It also contradicted the thing Rheana liked about the
      reference site: not saying many many figures at once. And its own numbers
      were the weak kind: 2,320 pull requests is volume rather than outcome,
      96.5% needs domain knowledge to read, and 4 companies looks small beside
      a timeline that names them. Three rebuilds in three days was the signal
      that the element was wrong rather than badly executed. The hero now runs
      straight into the work.

B-23  The toolshed said no to things the resume proves she did.
      Audited every technology named in the resume prose against what the
      search could find. Missing: BERT, RoBERTa, DistilBERT and GPT-2, on a
      resume whose research section is about them; Redmine, which the Azeus
      chatbot was built over; Elementor; OpenAPI; Tkinter; NanoGPT. Also
      "huggingface" typed without the space.
      Two fixes. Spellings joined the alias list. And the search now falls
      through to the resume's own prose when the chips have no answer, so it
      replies "I used it on Azeus Systems" instead of "not on the list" - same
      single source, nothing added to the CV, and the answer names the work.
      Whole-word matching only: without it "excel" matches "excellent" and the
      site starts claiming a skill off the back of an adjective.

D-04  PowerShell and Vite added at the source, not to the site.
      Both were provably used - this site is built with Vite, rayflow is
      PowerShell - and neither was anywhere in the resume, so the toolshed
      answered "not on the list". Added to skill_lines in resume.source.yaml
      and rebuilt: 98 raysume tests pass and the one-page variant still fits on
      one page.

B-24  The downloadable CV had been stale for three days.
      sync-content.mjs copied the JSON and not the PDF, so the site was
      generated from a resume the visitor could not download. Two artefacts out
      of one build must move together or they drift. The sync now copies both
      and fails loudly if the PDF is missing.

B-25  The border around the glasses was a focus ring.
      The gate focused the glasses button when it opened, so the browser drew a
      focus rectangle around the graphic - the first thing anyone saw. The
      dialog now takes focus instead, which is the correct target for a modal
      anyway: a screen reader reads it from the top rather than starting
      halfway down at a control. A keyboard user tabbing to the glasses still
      gets a clear indicator, rounded to follow the button.

D-05  New glasses, smaller.
      The old pair was two stroked rectangles, which read as two empty boxes -
      a border, where the thing being drawn is a lens. Now round frames with
      the lenses filled like glass and one sweep of light across both, at 11rem
      instead of 20rem.

B-26  A regex tidy-up broke the gate stylesheet.
      Removing the skip rule with /\.gate__skip \{[^}]*\}/ also matched
      ".gate__skip {" inside ".gate.is-wearing .gate__skip {", so it deleted
      that rule's body and left a dangling selector. Sass failed with
      "expected selector" and seven theme tests skipped because there was no
      built CSS to read. A pattern written for one rule matched two; the fix
      was to repair it by hand and stop reaching for a regex to edit
      structured text.

D-06  Two slow blinks after the glasses go on, seen from behind the eyes.
      Four attempts before the right idea. The first three drew one enormous
      eyelid across the viewport; the fourth drew two eyes with lashes, side by
      side, and looked good.
      All four were wrong, and Rheana named why: the visitor has just put the
      glasses ON, so they ARE the eye. There is nobody to look at. Showing a
      picture of eyes is showing someone their own eyes from the outside.
      What it does now: fades to black, then the page arrives as the lids lift,
      twice. No illustration at all - two soft-edged dark masses meeting and
      parting, blurred hard, because your own eyelid is centimetres from your
      retina and never in focus. A crisp edge reads as a camera shutter. The
      fringe along the top outlives the lids by a moment: lashes hang in the
      top of your vision after the lid has gone, out of focus.
      Opening is slower than closing, which is how a blink actually goes.
      Sequence: glasses 1250ms, blink 1900ms, then the page. About 3.1 seconds
      on every visit. Worth watching for whether it wears thin.

M-09  Spent four rounds designing an eye before asking what the visitor is
      supposed to be. The brief said "the user is the one seeing" from the
      start; a first-person effect was never going to come out of drawing a
      face. Cheaper to establish the point of view than to keep adjusting the
      drawing.

B-27  The gate was invisible to a whole class of visitor.
      Rheana's friend never saw it. Not a fault: the gate refused to mount for
      anyone with prefers-reduced-motion, which is more people than it sounds -
      Reduce Motion on iOS, Remove animations on Android, Animation effects off
      in Windows accessibility, and Windows battery saver, which turns it on
      without telling anyone.
      Ruled out the other explanations first rather than guessing: the live
      bundle carries no trace of the old once-per-visitor check, and the HTML
      is only cached for ten minutes, so neither stale code nor a stale page
      could explain it. Reproduced it directly with the media query forced.
      The fix is not to override the setting. It is to notice the setting is
      about MOVEMENT: the statement, the glasses and the blur are content, and
      only the travel and the blink are motion. Everyone gets the gate; those
      visitors get it still.
      Measured after: 3249ms to the page normally, 406ms with reduced motion.

B-28  Five rules pointed at colours that do not exist.
      Found while fact-checking the migration document, not by looking at the
      site: --ink-teal in three places and --text-faint in two. Neither name
      has ever been defined in this palette. Nothing failed, because an
      undefined custom property invalidates the declaration at computed-value
      time and the colour simply inherits - the page looked nearly right.
      Both came from the same mistake. The rules originally read
      var(--ink-teal, var(--ink-purple)), which worked, and a search and
      replace rewrote them to var(--ink-teal), reading the first argument and
      discarding the fallback that was carrying them.
      Fixed to --ink-mint, which is the teal in this palette, and to
      --text-muted, which is defined and clears contrast where --text-faint
      would not have. A faint placeholder is an accessibility fault rather than
      a style choice, so the muted token is also the better answer.
      apps/web/test/custom-properties.test.js now fails if any var() names a
      property nothing defines, with the seven properties PetalBurst.jsx sets
      at runtime listed explicitly rather than the check loosened.

M-10  The contrast suite passed throughout. It asserts the pairings it is told
      about; it cannot know about a colour that was never defined. Fifty green
      assertions are not the same as a page whose colours all resolve.

